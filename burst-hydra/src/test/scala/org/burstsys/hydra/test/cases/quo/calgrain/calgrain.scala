/* Copyright Yahoo, Licensed under the terms of the Apache 2.0 license. See LICENSE file in project root for terms. */
package org.burstsys.hydra.test.cases.quo

/**
  * 1. `calendarHour(epoch:long):long` _slice/truncate milliseconds into calendar hours granularity_
  * 1. `calendarDay(epoch:long):long` _slice/truncate milliseconds into calendar day granularity_
  * 1. `calendarWeek(epoch:long):long` _slice/truncate milliseconds into calendar week granularity_
  * 1. `calendarMonth(epoch:long):long` _slice/truncate milliseconds into calendar month granularity_
  * 1. `calendarQuarter(epoch:long):long` _slice/truncate milliseconds into calendar quarter granularity_
  * 1. `calendarHalf(epoch:long):long` _slice/truncate milliseconds into calendar halfs granularity_
  * 1. `calendarYear(epoch:long):long` _slice/truncate milliseconds into calendar years granularity_
  */
package object calgrain {

}
